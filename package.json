{
  "name": "modelsindex-dev",
  "private": true,
  "type": "module",
  "devDependencies": {
    "@astrojs/cloudflare": "^14.3.1",
    "astro": "^7.3.2",
    "vitest": "^5.0.0",
    "wrangler": "^4.131.0"
  },
  "scripts": {
    "build": "bash scripts/build_dist.sh && astro build",
    "dev": "astro dev",
    "test": "vitest run",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  }
}
