{
  "name": "modelsindex-dev",
  "private": true,
  "devDependencies": {
    "wrangler": "^4.131.0"
  },
  "scripts": {
    "build": "bash scripts/build_dist.sh",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  }
}
