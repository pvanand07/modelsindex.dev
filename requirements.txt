# crawl.py, gguf_header.py and bench.py are stdlib only.
# fit.py and build_index.py:
numpy>=1.24
# bench.py uses torch for the bandwidth test only if it is importable
# (the Modal GPU Image installs it).
# scripts/modal_app.py (local client):
modal>=1.0
# scripts/links.py's fetches (github/hf/homepage content). Not a hard requirement -- when
# missing, github/hf fall back to a bare urllib GET (they're plain API/CDN endpoints, no bot
# wall to route around) and homepage fetching falls back to Exa (EXA_API_KEY):
hrequests>=0.9
